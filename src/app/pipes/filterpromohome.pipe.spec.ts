import { FilterpromohomePipe } from './filterpromohome.pipe';

describe('FilterpromohomePipe', () => {
  it('create an instance', () => {
    const pipe = new FilterpromohomePipe();
    expect(pipe).toBeTruthy();
  });
});
